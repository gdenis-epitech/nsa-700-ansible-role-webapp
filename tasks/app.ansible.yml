- name: "Copy app to server"
  ansible.builtin.copy:
    src: files/app/
    dest: /app/
    directory_mode:
    owner: ansible
    group: ansible
    mode: "0755"

- name: "Install app packages"
  community.general.npm:
    path: /app
    state: present
  become: true
  become_user: ansible
